"use client";
import { useI18n } from "@/lib/i18n";
import { AreaChart, Area, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, BarChart, Bar, Cell } from "recharts";
import type { Trend, HazardCount } from "@/lib/types";
export function RiskTrend({ data }: { data: Trend[] }) {
  const { t } = useI18n(); return <div className="chart-frame" role="img" aria-label={t("Daily average risk score of active incidents over 14 days")}><ResponsiveContainer width="100%" height="100%" minWidth={0}><AreaChart data={data} margin={{ top: 12, right: 12, bottom: 0, left: -24 }}><defs><linearGradient id="riskFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#258c78" stopOpacity={0.19} /><stop offset="100%" stopColor="#258c78" stopOpacity={0.01} /></linearGradient></defs><CartesianGrid stroke="#edf0f1" vertical={false} /><XAxis dataKey="date" tickFormatter={(v: string) => v.slice(5).replace("-", "/")} tick={{ fontSize: 11, fill: "#859098" }} axisLine={false} tickLine={false} minTickGap={28} /><YAxis domain={[0, 100]} ticks={[0, 25, 50, 75, 100]} tick={{ fontSize: 11, fill: "#859098" }} axisLine={false} tickLine={false} /><Tooltip contentStyle={{ borderRadius: 10, border: "1px solid #e3e8e8", fontSize: 12 }} /><Area name={t("Risk score")} type="monotone" dataKey="risk_score" stroke="#258c78" strokeWidth={2.5} fill="url(#riskFill)" isAnimationActive={false} /></AreaChart></ResponsiveContainer></div>; }
export function HazardChart({ data }: { data: HazardCount[] }) {
  const { t } = useI18n(); return <div className="chart-frame" role="img" aria-label={t("Hazard count by type across all inspections")}><ResponsiveContainer width="100%" height="100%" minWidth={0}><BarChart data={data.map(d => ({ ...d, name: t(d.hazard_type) }))} layout="vertical" margin={{ top: 8, right: 25, left: 0, bottom: 0 }}><CartesianGrid stroke="#edf0f1" horizontal={false} /><XAxis type="number" allowDecimals={false} axisLine={false} tickLine={false} tick={{ fontSize: 11, fill: "#859098" }} /><YAxis type="category" dataKey="name" width={139} axisLine={false} tickLine={false} tick={{ fontSize: 11, fill: "#59656e" }} /><Tooltip cursor={{ fill: "#f4f7f7" }} contentStyle={{ borderRadius: 10, fontSize: 12 }} /><Bar dataKey="count" name={t("Observations")} radius={[0, 4, 4, 0]} barSize={15} isAnimationActive={false}>{data.map((d, i) => <Cell key={d.hazard_type} fill={i === 0 ? "#258c78" : i === 1 ? "#72b5a7" : "#b5cfc7"} />)}</Bar></BarChart></ResponsiveContainer></div>; }
